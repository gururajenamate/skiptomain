_schema-version: "3.2"
ID: skiptomain
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: skiptomain-destination-content
  type: com.sap.application.content
  requires:
  - name: skiptomain-destination-service
    parameters:
      content-target: true
  - name: skiptomain-repo-host
    parameters:
      service-key:
        name: skiptomain-repo-host-key
  - name: skiptomain-uaa
    parameters:
      service-key:
        name: skiptomain-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: skiptomain_html_repo_host
          ServiceInstanceName: skiptomain-html5-srv
          ServiceKeyName: skiptomain-repo-host-key
          sap.cloud.service: skiptomain
        - Authentication: OAuth2UserTokenExchange
          Name: skiptomain_uaa
          ServiceInstanceName: skiptomain-xsuaa-srv
          ServiceKeyName: skiptomain-uaa-key
          sap.cloud.service: skiptomain
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: skiptomain-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: skiptomain-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - skiptomain.zip
      name: skiptomain
      target-path: resources/
- name: skiptomain
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: skiptomain-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: skiptomain-destination-service
    service-plan: lite
- name: skiptomain-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: skiptomain-xsuaa-srv
    service-plan: application
- name: skiptomain-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: skiptomain-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
